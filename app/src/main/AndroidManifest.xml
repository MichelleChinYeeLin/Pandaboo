<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pandaboo">

    <application
        android:name=".AppLifeCycle"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeMain"></activity>
        <activity android:name=".Panda"></activity>
        <activity android:name=".Shop"></activity>
        <activity android:name=".Settings"></activity>
        <activity android:name=".TaskView"></activity>
        <activity android:name=".TaskAdd"></activity>
        <activity android:name=".Task"></activity>
        <activity android:name=".PlannerView"></activity>
        <activity android:name=".EventAdd"></activity>

        <receiver android:name=".AlarmReceiver"
            android:exported="true"
            android:enabled="true">

        </receiver>
    </application>

    <uses-permission android:name="android.permission.INTERNET"/>

</manifest>